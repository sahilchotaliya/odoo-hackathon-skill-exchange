package com.skillswap.security;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {
    // All security configuration has been moved to WebSecurityConfig
    // This class can be removed if not needed for other purposes
}